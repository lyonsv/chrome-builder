---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content.js
  - popup.js
  - site4source.html
  - output.json
autonomous: true
requirements:
  - TRACK-03
must_haves:
  truths:
    - "Searching the entire codebase for site4, site1, or site2 returns zero matches in any .js, .html, or .json file"
    - "All detection logic describes observable signals only (global variables, DOM attributes, script URL patterns) with no site-name-wrapping conditions or comments"
    - "The file site4source.html no longer exists at that path (renamed or deleted)"
    - "output.json is removed from the repository"
  artifacts:
    - path: "content.js"
      provides: "Detection logic with generic observable-pattern comments only"
      contains: "Detect component experience registry (observable:"
    - path: "popup.js"
      provides: "Detection logic with generic observable-pattern comments only"
      contains: "Detect component experience registry (observable:"
    - path: "test-page.html"
      provides: "Generic test fixture (renamed from site4source.html)"
  key_links:
    - from: "content.js:337"
      to: "observable pattern comment"
      via: "comment replacement"
      pattern: "Detect component experience registry"
    - from: "popup.js:484"
      to: "observable pattern comment"
      via: "comment replacement"
      pattern: "Detect component experience registry"
---

<objective>
Remove all hardcoded site names from the codebase. Two comment references name site4 explicitly (content.js:337, popup.js:484). The file site4source.html uses a site name in its filename and is publicly visible in the repo. output.json contains Site2 page content and URLs throughout.

Purpose: TRACK-03 — all detection logic must be expressed as generic observable patterns. No site names anywhere in shipped code.
Output: Clean codebase where `grep -ri "site4|site1|site2"` returns zero matches; detection comments describe signals not sites.
</objective>

<execution_context>
@/Users/vincent.lyons/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.lyons/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scrub site-name comments from content.js and popup.js</name>
  <files>content.js, popup.js</files>
  <action>
Replace the two named comment references with generic observable-pattern descriptions.

In content.js at line 337, replace:
  `// Component-based data architecture (like what we see in site4)`
With:
  `// Detect component experience registry (observable: global object with 10+ component keys)`

In popup.js at line 484, replace:
  `// Component-based architecture detection (like site4)`
With:
  `// Detect component experience registry (observable: global object with 10+ component keys)`

Do NOT change any surrounding logic — only the comment text on those exact lines.

After editing, run the audit grep to confirm zero matches:
  `grep -ri "site4|site1|site2" . --include="*.js" --include="*.html" --include="*.json" | grep -v ".planning" | grep -v "node_modules"`

If the grep returns any match from .js or .html files, fix it before marking done.
  </action>
  <verify>
    <automated>grep -ri "site4\|site1\|site2" /Users/vincent.lyons/git/chrome-builder --include="*.js" --include="*.html" | grep -v ".planning" | grep -v "node_modules"; echo "exit:$?"</automated>
  </verify>
  <done>grep returns zero matches in .js and .html files for site4, site1, or site2 (case-insensitive)</done>
</task>

<task type="auto">
  <name>Task 2: Remove output.json and rename site4source.html</name>
  <files>output.json, site4source.html, test-page.html</files>
  <action>
Two files in the repo violate TRACK-03:

1. **output.json** — contains full Site2 page content and hundreds of Site2 URLs. Delete this file entirely. Do not gitignore it; delete it. A sample file containing real site data is a liability.

2. **site4source.html** — uses a site name in its filename and is publicly visible. A `test-page.html` already exists in the repo root. Check whether `site4source.html` is distinct from `test-page.html`:
   - If `site4source.html` contains site-specific content (site4 URLs, branding, etc.), delete it.
   - If `site4source.html` is a useful generic test fixture (no site-specific content), rename it to `test-fixture-alt.html`.
   - In either case, the filename `site4source.html` must not exist after this task.

After completing both deletions/renames, run the full audit:
  `grep -ri "site4|site1|site2" . --include="*.js" --include="*.html" --include="*.json" | grep -v ".planning" | grep -v "node_modules"`

Zero matches required.
  </action>
  <verify>
    <automated>ls /Users/vincent.lyons/git/chrome-builder/output.json 2>/dev/null && echo "FAIL: output.json still exists" || echo "PASS: output.json removed"; ls /Users/vincent.lyons/git/chrome-builder/site4source.html 2>/dev/null && echo "FAIL: site4source.html still exists" || echo "PASS: site4source.html removed"</automated>
  </verify>
  <done>output.json deleted from repo; site4source.html renamed or deleted; grep audit returns zero matches across all .js, .html, .json files</done>
</task>

</tasks>

<verification>
Full TRACK-03 audit — run after both tasks complete:

```bash
grep -ri "site4\|site1\|site2" /Users/vincent.lyons/git/chrome-builder \
  --include="*.js" --include="*.html" --include="*.json" \
  | grep -v ".planning" | grep -v "node_modules"
```

Expected: no output (zero matches).
</verification>

<success_criteria>
- grep returns zero matches for site4, site1, site2 (case-insensitive) in all .js, .html, .json files
- content.js:337 comment reads "Detect component experience registry (observable: ...)"
- popup.js:484 comment reads "Detect component experience registry (observable: ...)"
- output.json does not exist
- site4source.html does not exist
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
